# v4.107 更新介绍（2026-09-02）

> 本次更新核心主题：**导演台全面对话化**——像 Pavo 一样，用一个「导演 Agent」在对话框里指挥整个视频创作流程。

---

## 🎬 重磅：导演 Agent（对话式导演台）

### 底部独立对话条（v4.107）
导演台底部新增**常驻对话条**，在对话框里直接指挥导演台，与主聊天模块**零交集**：

| 你说 | 会发生什么 |
|---|---|
| 「第3镜的关键帧改成夜晚」 | 只重生成那一张关键帧，就地替换 |
| 「主角换成短发戴眼镜」 | 重生成三视图，后续分镜自动跟随新形象 |
| 「第2镜改成先回头再说话」 | 整镜替换文本并重新生成视频 |
| 「合成成片」 | 合并输出最终视频 |
| 「现在到哪一步了」 | 回报进度（几镜、关键帧/片段生成状态） |

- **隔离设计**：不写主会话历史、不污染长期记忆、只挂 5 个 `director_*` 白名单工具（删文件/执行命令等高危工具一律不给）
- 对话历史跟随项目存档（`director_chat.json`），换项目自动切换，重开可续聊
- 「清空」按钮一键重置对话，方便反复试
- 在主聊天框说导演指令，会引导你到导演台对话条——两个模块互不干扰

### 5 个导演工具（v4.106）
新增 `director_status` / `director_revise_clip` / `director_revise_keyframe` / `director_revise_character` / `director_merge`，支持整镜替换、单张关键帧重生成、角色三视图重生成（含修改意见注入）。

## 🖼️ 导演台预览区网页化（v4.105）
四个预览容器（角色三视图 / 关键帧 / 分镜 / 合成）由静态卡片升级为网页渲染：
- 视频**点击即播**（▶ 播放图标 + 系统播放器弹窗，支持进度条/暂停/音量）
- 图片点击灯箱放大
- 自定义 `localres://` scheme 绕开 file:// CORS，本地资源秒开
- 生成过程自动刷新，改完立刻看得见

## 🎨 生图 / 视频模型升级
- **生图**：升级 `agnes-image-2.5-flash`，支持精确尺寸档位转换（2K/比例自适应）+ 防乱加字约束
- **视频**：升级 `agnes-video-2.5-flash`（720P、4~12 秒），新版不再自动加 ASR 乱码字幕，画面干净无需裁底
- 生图链路四项修复回归：progress 透传、尺寸档位/比例转换、防加字、精确尺寸对齐

## 🐛 重要修复
- **导演台视频合成无声 BUG**（08-31 遗留）：根因是冻结环境 GBK locale 下 ffmpeg UTF-8 输出解码崩溃导致音轨误判；改为 bytes 捕获 + 乐观探测策略，并加合成后音量自检
- **导演台对话条流式渲染重复累加**：「镜镜3镜3关键镜3关键帧…」灾难——累积文本被当增量追加，已改替换语义渲染
- **分镜视频预览点不动**：自定义 scheme 不支持 HTTP Range，改为点击调系统播放器
- **「开始导演」点击无反应**：懒加载 import 引用已移除符号，ImportError 被 Qt slot 静默吞掉
- **浏览器接管模式**：CDP 复用当前页 + contenteditable 兼容 + selector 鲁棒性
- main.py Obsidian 初始化线程缺 import 导致知识库索引静默失败

## 🏗️ 架构与基建
- **对话渲染重构**：聊天区由 QTextBrowser 换 QWebEngineView（Markdown 渲染、流式平滑、修复重复气泡）
- **浏览器桥接**：`browser_bridge` 本地服务（token 鉴权 + 仅绑回环）+ MV3 扩展，`browser_runner --cdp` 可接管真实浏览器继承登录态
- **AgentWorker 隔离会话**（可复用架构能力）：`isolated=True` 即开独立会话——不回写主会话/不写长期记忆/不落盘轨迹，仅保留交付物登记
- **技能系统**：安装 / 市场 / 审查 / 管理 UI
- **Agent 核心增强**：长任务续跑 checkpoint、轨迹记忆、步数预算收紧（最少步骤铁律）
- **数字人面板**：头像目录迁移、对口型生成线程、AI 标识烧录（合规硬性要求）
- **工程验证基建**：PyInstaller PYZ 硬核实入包校验（字节码级断言，防"打包成功但改动没进包"），本次共修复 4 个验证脚本的过期断言

---

## 升级方式

重启新版 exe 即可。导演台对话条在导演台页面底部常驻，无需任何配置。

**完整提交**：`deb5246…aa18290`（35 commits，2026-09-02）
