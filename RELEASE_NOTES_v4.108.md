# v4.108 更新介绍（2026-09-03）

> 本次更新核心主题：**全面代码审查后的稳定性大扫除**——第三方对 v4.107 做了逐行代码审查（61 项问题：16 高 / 28 中 / 17 低），本轮按严重程度 P0→P3 全部处理（修 50+ 项），并修复了聊天渲染状态机的三个深层缺陷。叠加 9/2 的「导演台对话化」，本版本是 v4.105→v4.108 的完整累积。

---

## 🎬 重磅回顾：导演 Agent（对话式导演台，v4.105-107）

### 底部独立对话条
导演台底部**常驻对话条**，在对话框里直接指挥导演台，与主聊天模块**零交集**：

| 你说 | 会发生什么 |
|---|---|
| 「第3镜的关键帧改成夜晚」 | 只重生成那一张关键帧，就地替换 |
| 「主角换成短发戴眼镜」 | 重生成三视图，后续分镜自动跟随新形象 |
| 「第2镜改成先回头再说话」 | 整镜替换文本并重新生成视频 |
| 「合成成片」 | 合并输出最终视频 |
| 「现在到哪一步了」 | 回报进度（几镜、关键帧/片段生成状态） |

- 5 个 `director_*` 白名单工具；对话历史跟随项目存档、可清空重来
- 主聊天框说导演指令会被引导到导演台，两模块互不干扰

### 预览区网页化 + 模型升级
- 四预览容器网页渲染：视频点击即播（系统播放器）、图片灯箱、`localres://` 秒开
- 生图 `agnes-image-2.5-flash`（尺寸档位/防加字）；视频 `agnes-video-2.5-flash`（720P、无 ASR 乱码字幕）

---

## 🛡️ v4.108 全面 BUG 大扫除（本次核心）

第三方逐行代码审查后按 P0→P3 全部修复，聚焦**失败路径**——正常路径此前没问题，但出错时错误被吞、被谎报、被错格式化反而制造更大故障。代表性修复：

### Agent 核心（400 死局 → 会话不再"带毒"）
- **消息配对断裂修复**（反复 HTTP 400 的根因）：工具回执 id 取错层级、工作流早退缺回执、并发停止跳过回执——三路补全，`assistant.tool_calls` 与 `tool` 回执严格配对
- 断点续跑真正可恢复：检查点**原子写** + 每步落最新消息快照 + 心跳；续跑不再丢失高级模型开关（弱模型退化成"文字演工具"的通道堵死）
- 内部指令（督促/防撒谎/自检等）不再污染可见会话历史，也不会被当用户发言回喂模型

### 界面与交互
- 气泡操作按钮（🔄 重新生成 / ✏️ 改写 / 👍 / 👎）全面复活（WebEngine 迁移遗留）
- 全局唤起热键 Ctrl+Alt+X 修复；无边框窗口边缘缩放修复（高分屏 DPI 换算）；关窗不再挂死
- **聊天渲染状态机重构**：回答落地不再整页重排（工具卡不丢）、工具卡 id 全局唯一（多轮/并发不串卡）、流式不再产生同段双气泡
- 导演台：失败如实报错（不再谎报"已完成"）、合成中途可点停止、角色修改意见只保留最新一条、全部重生成即时刷新卡片、并发指令互斥、首帧抽帧缓存不卡 UI

### 数据与安全
- 记忆库并发加锁；会话 ID 防同秒覆盖丢档；config / 检查点原子保存防损坏
- sessions.json 体积控制（工具日志截断存储）
- Webhook 服务安全收口：默认仅本机回环 + 共享 token 校验 + 请求体限长
- 浏览器工具链中文乱码修复（子进程统一 UTF-8）；跨盘符路径崩溃修复
- 浏览器扩展源码入库并随包分发（换机/开源安装说明不再指向不存在的文件）

---

## 🏗️ 工程验证基建

- **PyInstaller PYZ 硬核实入包校验**（字节码级断言）持续增强：修复 4 个回归脚本的过期/错误断言（`find_module` 精确匹配、tuple 常量展开、`co_varnames` 形参判定、6.19 `ZlibArchiveReader` API 变更）
- 新增 v4.108 修复定向测试（14 组行为断言）与 M-25 渲染状态机测试

---

## 升级方式

重启新版 exe 即可。设置页显示 **v4.108 / 构建 2026-09-03**。

**完整提交**：`1652d86` `d37e1e3` `903b9cd` + 9/2 共 35 commits（`deb5246…aa18290`）
