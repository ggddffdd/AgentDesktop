# 小臭玩AI v4.107 BUG 修复工单

> 生成：2026-09-03 01:30，来源：冰咖啡全面代码审查（三路子代理深读 + 静态扫描交叉复核）
> 对象：本目录（deepseek-desktop）v4.107 未提交代码
> 性质：只修本单列出的问题，最小 diff。禁止重构、禁止改架构、禁止动 dist/ build/ _obsolete*

## 执行要求
1. 按顺序先修 P0（8 项），再修 P1（9 项）；P2 本轮不做（除非顺手且零风险）
2. 行号基于 2026-09-02 代码状态，若有偏移按问题描述+代码内容定位
3. 每修完一个文件，运行 `python -m py_compile <文件>` 验证
4. 全部修完后输出清单：每项标注 [已修] / [跳过+原因]
5. agent.py 的配对类修复务必自查：每条 assistant 消息里的 tool_calls，后续必须紧跟同 id 的 tool 角色回执，否则 API 400

---

## P0 —— 必现硬伤（先修）

### P0-1 agent.py:1341 / agent.py:1373 —— tool_call_id 写错
- 问题：护栏/回执代码用 `fn.get("id")` 取 id（fn 是 function 对象），与 tool_calls.id 不配对 → API 返回 400，对话死锁
- 修复：改为 `tc.get("id")`（tc 为 tool_calls 列表元素本身）。两处都改，并 grep 确认无第三处同样写法

### P0-2 agent.py:1273 / 1303 附近 —— run_workflow 早退缺占位回执
- 问题：workflow 提前 return 的路径上，assistant 消息已带 tool_calls，但没有 tool 角色占位回执，下一轮请求必 400
- 修复：早退前为每个未完成的 tool_call 补 `{"role":"tool","tool_call_id":...,"content":"[workflow interrupted]"}`

### P0-3 director_panel.py:1034 —— clip_failed 信号 lambda 丢参
- 问题：信号带参发出，连接用零参 lambda，触发 TypeError
- 修复：lambda 补对应形参，或改用带参槽函数

### P0-4 director_panel.py:209 —— 未定义变量 idx
- 问题：`idx` 未定义，应为实例属性 `self.idx`（以实际上下文为准）
- 修复：改为 self.idx 或补定义

### P0-5 ui.py:4252 —— anchorClicked 把 QUrl 当 str
- 问题：anchorClicked(QUrl) 回调里对 url 直接调用 str 方法，QUrl 无此方法则崩
- 修复：回调入口统一 `url = url.toString()` 转 str 再处理

### P0-6 ui.py:1238 / 1240 —— 常量名拼错
- 问题：引用 `_HTBOTTOMLEFT` / `_HTBOTTOMRIGHT`，实际定义为 `_HT_BOTTOMLEFT` / `_HT_BOTTOMRIGHT`
- 修复：两处引用补下划线（以实际定义为准）

### P0-7 browser_runner.py:200 —— inner_text() 缺必填参数
- 问题：调用 inner_text() 未传必填的 selector 参数
- 修复：按函数签名补齐参数

### P0-8 tools.py:899 —— 未定义 resolve_python
- 问题：`resolve_python` 未定义/未导入
- 修复：项目内已有同名函数则补 import，否则补定义

---

## P1 —— 高危逻辑缺陷

### P1-1 director_panel.py:1587-1608 —— _director_reset 不等待旧线程
- 问题：重置时旧工作线程未退出就启动新一轮，两轮并发操作同一状态
- 修复：旧线程 join(timeout) 或原子标志互斥，确保旧线程完全停止后再开新线程

### P1-2 director_chat.py:149-197 —— 被拒绝的指令也入库
- 问题：指令被拒绝后仍写入会话存储，历史被污染
- 修复：拒绝路径不入库，或打 rejected 标记并在渲染/上下文组装时过滤

### P1-3 director_panel.py:738-752 —— 工具失败也回填 ok:True
- 问题：回填不区分成败，失败也标成功，导演台据此做错误决策
- 修复：按真实执行结果回填 ok 与错误信息

### P1-4 ui.py:1437 + task_resume.py —— 断点续跑空壳
- 问题：续跑入口存在但恢复逻辑未实现，点击后任务不会真正继续
- 修复：实现"恢复会话状态+从未完成步骤继续"；短期无法实现则入口明确提示"暂不可用"，禁止静默假动作

### P1-5 ui.py:6973-6985 —— 流式失败被静默吞掉
- 问题：流式请求异常路径没有通知 UI，用户只看到"没反应"
- 修复：except 分支向 UI 发错误信号/插入错误气泡，并恢复输入框可用状态

### P1-6 main.py:366 —— 跨线程 QTimer.singleShot
- 问题：非 GUI 线程调用 QTimer.singleShot，定时器可能永不触发
- 修复：改用跨线程信号槽，或 QMetaObject.invokeMethod(..., Qt.QueuedConnection)

### P1-7 browser_control_tools.py:163 × browser_runner.py:214 —— 编码不一致
- 问题：一侧 GBK 一侧 UTF-8，中文路径/内容必乱码或 UnicodeDecodeError
- 修复：读写两端统一显式 encoding='utf-8'

### P1-8 tools.py:56 / 796 / 1061 / 1341 / 1456 —— 跨盘符 relpath 崩溃
- 问题：os.path.relpath 跨盘符（C:↔D:）抛 ValueError
- 修复：封装安全 relpath（ValueError 时回退返回绝对路径），5 处统一替换

### P1-9 agent.py:1303-1320 —— run_workflow 早退语义（与 P0-2 一并处理）
- 问题：早退分支直接 return，未处理进行中的工具调用与状态
- 修复：补占位回执 + 保存可恢复状态（或明确标记该轮 workflow 已终止）

---

## P2 —— 本轮不做，仅备忘
- webhook_server.py：无鉴权 + 默认 0.0.0.0:9000 + body 无上限（建议 token 校验 + 默认 127.0.0.1 + 限长）
- browser_extension 未进打包 spec（小臭玩AI.spec datas）→ 新机器缺扩展文件
- memory_store.py append_memory 新旧两版共存（453/687）→ 删旧版
- 其余中危 16 项：见完整审查报告（HTML，协作空间存档）

## P3 —— 卫生项
- 根目录 32 个 `_` 前缀临时脚本 + 56 个 test_/verify_ 文件 → 建议归档 tests/ 或清理
- 其余低危 22 项：见完整审查报告

## 验收
1. 所有涉及文件 `python -m py_compile` 通过
2. P0-1/P0-2 修复后自查 tool_calls↔tool 回执配对
3. 输出修复清单：[已修] / [跳过+原因]
